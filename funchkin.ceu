native do
  struct Funchkin {
    // absolute
    float latitude, longitude;
    int id;

    // relative to self
    unsigned long distance, previousDistance;
    int bearing, direction, distanceTier;
  };
end