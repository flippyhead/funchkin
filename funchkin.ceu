native do
  struct Funchkin {
    // absolute
    float latitude, longitude;
    int id;

    // relative to self
    unsigned int distance;
    int bearing, direction, distanceTier, previousDistanceTier;
  };
end