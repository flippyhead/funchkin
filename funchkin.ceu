native do
  struct Funchkin {
    // absolute
    float latitude, longitude;
    int id;

    // relative to self
    unsigned int distance, previousDistance;
    int bearing, direction, distanceTier;
  };
end